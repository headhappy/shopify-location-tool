name: Retired location v3 patcher
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo 'This patcher is retired. All location tools use the shared variant-only four-field implementation. No source files are changed.'
